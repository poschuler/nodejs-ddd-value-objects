name: nodejs-ddd-value-objects

services:
  structurizr:
    image: structurizr/structurizr:latest
    container_name: structurizr
    command: local
    user: "1000:1000"
    restart: unless-stopped
    ports:
      - "8081:8080"
    volumes:
      - ./architecture:/usr/local/structurizr
